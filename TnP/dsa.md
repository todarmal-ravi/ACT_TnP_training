list: []
dynamic, heterogenous, can have duplicates, indexed access

tuples: ()
static, heterogenous, can have duplicates, indexed access [immutable, hence used internally by python]

set: {}
dynamic, heterogenous, cannot have duplicates, no indexed access [filteration, to remove duplicates]

dict: (key:value)
dynamic, heterogenous, can have duplicates values, no indexed access

<h1>9821601163</h1>

<h2>class</h2>
virtual idea
gives framework
has properties(column headings)
has operations to access

<h2>>object</h2>
physical entity
implementation of frame work
has values(column values at each row)
methods to set/access values

public: to all "var"
private: accessed only to members "__var" (cannot be inherited)
protected: accessed onlt to "_var"

<h2>Types of Inheritance</h2>
single/simple
multi-level
multiple
hierarchical
multiple (java does not support this) (first come first serve in python, which func inherited first will be considered)
hybrid 

<p>sub-class function will always override the same function of parent class</p>

<p><strong>ADT</strong> is set of operations done on data structure, that must be present to implement data structure</p>